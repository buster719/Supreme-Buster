# Buster Local Repository Security Audit

- contract_id: sr-1781083039-f3a01a7f4973b583
- mode: LocalRepositoryAudit
- authorization_basis: owned local Buster repository
- target_scope: ../buster-core
- finding_count: 104

## Findings

- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:14 let api_key = std::env::var("OPENROUTER_API_KEY") | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:409 println!("registered secret handle: {}", openrouter.0); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:413 println!("plaintext secret exposed: no"); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:432 let leaked_line = "adapter log accidentally included sk-demo-openrouter-secret"; | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:478 println!("local secret store: {}", store_path.display()); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:483 println!("broker received secret shape: {header_shape}"); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [low][network egress review] buster-core/crates/buster_body/src/bin/buster-body.rs:579 "https://evil.example/api", | mitigation: confirm host is allowlisted and request goes through the egress broker
- [medium][process or shell boundary review] buster-core/crates/buster_body/src/cli_tool.rs:157 let mut command = Command::new(&self.command.command); | mitigation: confirm arguments are structured and scoped to Buster-owned processes or hosts
- [medium][process or shell boundary review] buster-core/crates/buster_body/src/cli_tool.rs:310 CliToolCommand::new("cmd") | mitigation: confirm arguments are structured and scoped to Buster-owned processes or hosts
- [medium][process or shell boundary review] buster-core/crates/buster_body/src/cli_tool.rs:317 CliToolCommand::new("/bin/sh") | mitigation: confirm arguments are structured and scoped to Buster-owned processes or hosts
- [medium][process or shell boundary review] buster-core/crates/buster_body/src/cli_tool.rs:326 CliToolCommand::new("cmd") | mitigation: confirm arguments are structured and scoped to Buster-owned processes or hosts
- [medium][process or shell boundary review] buster-core/crates/buster_body/src/cli_tool.rs:333 CliToolCommand::new("/bin/sh") | mitigation: confirm arguments are structured and scoped to Buster-owned processes or hosts
- [medium][secret handling review] buster-core/crates/buster_body/src/gate.rs:103 Self::Secret(reason) => write!(formatter, "body gate secret denied: {reason:?}"), | mitigation: confirm only secret handles are logged and redaction covers exact values
- [low][network egress review] buster-core/crates/buster_body/src/gate.rs:369 .preflight_network(&broker, "https://evil.example/api") | mitigation: confirm host is allowlisted and request goes through the egress broker
- [medium][secret handling review] buster-core/crates/buster_body/src/ironclaw_secret_backend.rs:23 write!(formatter, "IronClaw secret `{}` is missing", handle.0) | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/ironclaw_secret_backend.rs:26 write!(formatter, "IronClaw secret store unavailable: {reason}") | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/ironclaw_secret_backend.rs:69 fn get_secret(&self, handle: &SecretHandle) -> Option<String> { | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/llm.rs:210 Self::SecretBroker { message } => write!(formatter, "secret broker error: {message}"), | mitigation: confirm only secret handles are logged and redaction covers exact values
- [low][network egress review] buster-core/crates/buster_body/src/llm.rs:252 pub const DEFAULT_ENDPOINT: &'static str = "https://openrouter.ai/api/v1/chat/completions"; | mitigation: confirm host is allowlisted and request goes through the egress broker
- [medium][secret handling review] buster-core/crates/buster_body/src/llm.rs:260 let api_key = env::var(env_var).map_err(|_| LlmError::MissingApiKeyEnv { | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/local_secret_store.rs:40 Self::Io(reason) => write!(formatter, "secret store io error: {reason}"), | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/local_secret_store.rs:41 Self::Serde(reason) => write!(formatter, "secret store format error: {reason}"), | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/local_secret_store.rs:42 Self::Crypto(reason) => write!(formatter, "secret store crypto error: {reason}"), | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/local_secret_store.rs:43 Self::Decode(reason) => write!(formatter, "secret store decode error: {reason}"), | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/local_secret_store.rs:44 Self::Missing { handle } => write!(formatter, "secret `{}` is missing", handle.0), | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/local_secret_store.rs:92 pub fn get_secret_result( | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/local_secret_store.rs:143 fn write_file(&self, file: &SecretStoreFile) -> Result<(), LocalSecretStoreError> { | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/local_secret_store.rs:150 fs::write(&self.path, raw).map_err(|error| LocalSecretStoreError::Io(error.to_string())) | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/local_secret_store.rs:163 fn get_secret(&self, handle: &SecretHandle) -> Option<String> { | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/local_secret_store.rs:164 self.get_secret_result(handle).ok() | mitigation: confirm only secret handles are logged and redaction covers exact values
- [low][panic surface review] buster-core/crates/buster_body/src/local_secret_store.rs:223 let raw = fs::read_to_string(&path).unwrap(); | mitigation: confirm panic cannot be triggered by untrusted input or corrupt state
- [medium][secret handling review] buster-core/crates/buster_body/src/local_secret_store.rs:228 store.get_secret(&handle), | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/local_secret_store.rs:272 assert!(wrong_store.get_secret_result(&handle).is_err()); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][process or shell boundary review] buster-core/crates/buster_body/src/master_key.rs:114 let mut child = Command::new(&self.powershell) | mitigation: confirm arguments are structured and scoped to Buster-owned processes or hosts
- [medium][secret handling review] buster-core/crates/buster_body/src/master_key.rs:288 function Write-BusterCredential([string]$targetName, [string]$secretText) { | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/master_key.rs:322 Write-BusterCredential $target $secretText | mitigation: confirm only secret handles are logged and redaction covers exact values
- [low][network egress review] buster-core/crates/buster_body/src/network.rs:349 .preflight_url("https://evil.example/api") | mitigation: confirm host is allowlisted and request goes through the egress broker
- [low][network egress review] buster-core/crates/buster_body/src/network.rs:364 .preflight_url("http://127.0.0.1:8080/private") | mitigation: confirm host is allowlisted and request goes through the egress broker
- [low][network egress review] buster-core/crates/buster_body/src/network.rs:386 "https://evil.example/api", | mitigation: confirm host is allowlisted and request goes through the egress broker
- [low][panic surface review] buster-core/crates/buster_body/src/persistence.rs:505 let raw = fs::read_to_string(root.join("audit/body-events.jsonl")).unwrap(); | mitigation: confirm panic cannot be triggered by untrusted input or corrupt state
- [low][panic surface review] buster-core/crates/buster_body/src/persistence.rs:535 let raw = fs::read_to_string(root.join("audit/tool-actions.jsonl")).unwrap(); | mitigation: confirm panic cannot be triggered by untrusted input or corrupt state
- [medium][process or shell boundary review] buster-core/crates/buster_body/src/process_manager.rs:122 let mut command = Command::new(&request.command); | mitigation: confirm arguments are structured and scoped to Buster-owned processes or hosts
- [low][panic surface review] buster-core/crates/buster_body/src/repair.rs:102 fs::read_to_string(root.join("BODY.md")).unwrap(), | mitigation: confirm panic cannot be triggered by untrusted input or corrupt state
- [medium][process or shell boundary review] buster-core/crates/buster_body/src/script_sandbox.rs:55 let mut command = Command::new(&self.config.command); | mitigation: confirm arguments are structured and scoped to Buster-owned processes or hosts
- [low][network egress review] buster-core/crates/buster_body/src/script_sandbox.rs:179 "#!/usr/bin/env bash\nprintf 'NETWORK evil.example\\n'\nprintf 'DIRECT_NETWORK curl https://evil.example\\n'\nprintf 'sk-demo-redacted\\n'\n", | mitigation: confirm host is allowlisted and request goes through the egress broker
- [medium][secret handling review] buster-core/crates/buster_body/src/secrets.rs:302 fn get_secret(&self, handle: &SecretHandle) -> Option<String>; | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/secrets.rs:318 fn get_secret(&self, handle: &SecretHandle) -> Option<String> { | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/secrets.rs:371 .get_secret(&lease.handle) | mitigation: confirm only secret handles are logged and redaction covers exact values
- [low][network egress review] buster-core/crates/buster_daemon/src/executor.rs:760 .unwrap_or_else(|| "https://api.xiaomimimo.com/v1/chat/completions".to_string()); | mitigation: confirm host is allowlisted and request goes through the egress broker
- [medium][secret handling review] buster-core/crates/buster_daemon/src/executor.rs:852 .get_secret_result(&SecretHandle(handle.to_string())) | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_daemon/src/executor.rs:985 let old = env::var("OPENROUTER_API_KEY").ok(); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_daemon/src/executor.rs:988 let old_mimo_key = env::var("MIMO_API_KEY").ok(); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_daemon/src/executor.rs:989 let old_xiaomi_key = env::var("XIAOMI_API_KEY").ok(); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [low][panic surface review] buster-core/crates/buster_daemon/src/executor.rs:1050 fs::create_dir_all(audit.parent().unwrap()).unwrap(); | mitigation: confirm panic cannot be triggered by untrusted input or corrupt state
- [medium][secret handling review] buster-core/crates/buster_daemon/src/harness.rs:305 "respond to human dialogue without revealing secrets or hidden reasoning: {}", | mitigation: confirm only secret handles are logged and redaction covers exact values
- [low][panic surface review] buster-core/crates/buster_daemon/src/organism.rs:1015 let record: SignedEventRecord = serde_json::from_str(event).unwrap(); | mitigation: confirm panic cannot be triggered by untrusted input or corrupt state
- [medium][secret handling bypass switch] buster-core/crates/buster_daemon/src/security_research.rs:373 if line.contains("disable") && line.contains("env") && line.contains("secret") { | mitigation: ensure bypass switches are test-only or gated by BodyGate policy
- [medium][secret handling review] buster-core/crates/buster_daemon/src/security_research.rs:389 || line.contains("get_secret") | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_daemon/src/security_research.rs:395 "confirm only secret handles are logged and redaction covers exact values", | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][process or shell boundary review] buster-core/crates/buster_daemon/src/security_research.rs:398 if line.contains("command::new") || line.contains("start-process") || line.contains("ssh.exe") { | mitigation: confirm arguments are structured and scoped to Buster-owned processes or hosts
- [low][network egress review] buster-core/crates/buster_daemon/src/security_research.rs:405 if line.contains("http://") || line.contains("https://") { | mitigation: confirm host is allowlisted and request goes through the egress broker
- [medium][secret handling review] buster-core/crates/buster_daemon/src/security_research.rs:507 purpose: "Identify Buster-owned code paths where secrets, tools, network, or memory writes cross body boundaries.".to_string(), | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_daemon/src/security_research.rs:602 "fn demo() { let api_key = std::env::var(\"KEY\").unwrap(); println!(\"{}\", api_key); }", | mitigation: confirm only secret handles are logged and redaction covers exact values
- [low][network egress review] buster-core/crates/buster_daemon/src/source_fetch.rs:149 let mut url = reqwest::Url::parse("https://export.arxiv.org/api/query") | mitigation: confirm host is allowlisted and request goes through the egress broker
- [low][network egress review] buster-core/crates/buster_daemon/src/source_fetch.rs:174 reqwest::Url::parse("https://api.openalex.org/works").map_err(|e| e.to_string())?; | mitigation: confirm host is allowlisted and request goes through the egress broker
- [low][network egress review] buster-core/crates/buster_daemon/src/source_fetch.rs:192 reqwest::Url::parse("https://api.crossref.org/works").map_err(|e| e.to_string())?; | mitigation: confirm host is allowlisted and request goes through the egress broker
- [low][network egress review] buster-core/crates/buster_daemon/src/source_fetch.rs:748 <feed xmlns="http://www.w3.org/2005/Atom"> | mitigation: confirm host is allowlisted and request goes through the egress broker
- [low][network egress review] buster-core/crates/buster_daemon/src/source_fetch.rs:750 <id>http://arxiv.org/abs/2401.00001v1</id> | mitigation: confirm host is allowlisted and request goes through the egress broker
- [low][network egress review] buster-core/crates/buster_daemon/src/source_fetch.rs:771 "id": "https://openalex.org/W1", | mitigation: confirm host is allowlisted and request goes through the egress broker
- [low][network egress review] buster-core/crates/buster_daemon/src/source_fetch.rs:773 "doi": "https://doi.org/10.1234/example", | mitigation: confirm host is allowlisted and request goes through the egress broker
- [low][network egress review] buster-core/crates/buster_daemon/src/source_fetch.rs:777 "landing_page_url": "https://example.org/paper", | mitigation: confirm host is allowlisted and request goes through the egress broker
- [low][network egress review] buster-core/crates/buster_daemon/src/source_fetch.rs:798 "URL": "https://doi.org/10.5555/test", | mitigation: confirm host is allowlisted and request goes through the egress broker
- [low][network egress review] buster-core/crates/buster_daemon/src/source_fetch.rs:826 url: Some("https://example.org".to_string()), | mitigation: confirm host is allowlisted and request goes through the egress broker
- [low][network egress review] buster-core/crates/buster_daemon/src/source_fetch.rs:848 url: Some("https://example.org".to_string()), | mitigation: confirm host is allowlisted and request goes through the egress broker
- [low][network egress review] buster-core/crates/buster_daemon/src/web.rs:35 format!("http://{}:{}", self.host, self.port) | mitigation: confirm host is allowlisted and request goes through the egress broker
- [medium][process or shell boundary review] buster-core/crates/buster_daemon/src/web.rs:237 Command::new("cmd.exe") | mitigation: confirm arguments are structured and scoped to Buster-owned processes or hosts
- [medium][process or shell boundary review] buster-core/crates/buster_daemon/src/web.rs:241 } else if Command::new("cmd.exe") | mitigation: confirm arguments are structured and scoped to Buster-owned processes or hosts
- [medium][process or shell boundary review] buster-core/crates/buster_daemon/src/web.rs:248 Command::new("xdg-open").arg(url).spawn().map(|_| ()) | mitigation: confirm arguments are structured and scoped to Buster-owned processes or hosts
- [low][panic surface review] buster-core/crates/buster_dreaming/src/lib.rs:1085 let experience = fs::read_to_string(root.join("memory/experience.md")).unwrap(); | mitigation: confirm panic cannot be triggered by untrusted input or corrupt state
- [low][panic surface review] buster-core/crates/buster_dreaming/src/lib.rs:1086 let global_memory = fs::read_to_string(root.join("MEMORY.md")).unwrap(); | mitigation: confirm panic cannot be triggered by untrusted input or corrupt state
