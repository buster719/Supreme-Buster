# Buster Local Repository Security Audit

- contract_id: sr-1781082917-daf9d705bf97dd5e
- mode: LocalRepositoryAudit
- authorization_basis: owned local Buster repository
- target_scope: ../buster-core
- finding_count: 932

## Findings

- [low][panic surface review] buster-core/crates/buster_body/examples/body_supervisor_smoke.rs:23 let ticks = supervisor.run().expect("supervisor should run"); | mitigation: confirm panic cannot be triggered by untrusted input or corrupt state
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:9 BodyGate, InMemorySecretBackend, SecretBackend, SecretBroker, SecretClass, SecretHandle, | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:10 SecretRecord, SecretRegistry, SqliteBodyStore, | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:14 let api_key = std::env::var("OPENROUTER_API_KEY") | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:15 .expect("set OPENROUTER_API_KEY before running this development smoke test"); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:16 let secret_handle = SecretHandle("openrouter_api_key".to_string()); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:19 let mut registry = SecretRegistry::new(); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:20 registry.register(SecretRecord::new( | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:21 secret_handle.clone(), | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:22 SecretClass::LlmApiKey, | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:24 "OpenRouter API key stored behind Buster body secret broker", | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:26 let secret_lease = registry | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:28 &secret_handle, | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:34 .expect("secret lease should be issued"); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:36 let mut secret_backend = InMemorySecretBackend::new(); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:37 secret_backend.put_secret(secret_handle, api_key); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:38 let broker = Arc::new(Mutex::new(SecretBroker::new(registry, secret_backend))); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:48 BrokeredOpenRouterLlmBackend::new(broker, secret_lease.lease_id, capability, provider) | mitigation: confirm only secret handles are logged and redaction covers exact values
- [low][panic surface review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:50 let store = SqliteBodyStore::open_in_memory().expect("body gate store should open"); | mitigation: confirm panic cannot be triggered by untrusted input or corrupt state
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:59 "用一句中文说明 Buster 的 LLM 调用为什么要经过 SecretBroker。", | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:62 .with_secret() | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:77 .with_secret() | mitigation: confirm only secret handles are logged and redaction covers exact values
- [low][panic surface review] buster-core/crates/buster_body/examples/openrouter_broker_smoke.rs:93 .expect("audit count should be readable") | mitigation: confirm panic cannot be triggered by untrusted input or corrupt state
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_smoke.rs:11 OpenRouterClient::from_env().expect("set OPENROUTER_API_KEY before running this example"); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_smoke.rs:30 .with_secret() | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/examples/openrouter_smoke.rs:45 .with_secret() | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:16 LocalEncryptedSecretStore, ManagedUnit, MasterKeyProvider, NetworkEgressSensor, | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:18 ScriptSandbox, ScriptSandboxConfig, SecretBackend, SecretBroker, SecretClass, | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:19 SecretFingerprint, SecretHandle, SecretOutputSensor, SecretRecord, SecretRedactor, | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:20 SecretRegistry, SensorSignal, SensorSuite, SignalKind, StaticSignalSensor, SupervisorConfig, | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:62 "secret-demo" => run_secret_demo(), | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:63 "secret-store-demo" => { | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:65 run_secret_store_demo(&root) | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:260 "demo setup: simulated Buster-owned script attempts suspicious egress and secret output" | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:268 .push("suspicious network anomaly and secret exfiltration attempt".to_string()); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:279 .with_sensor(SecretOutputSensor::new(vec![OutputObservation { | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:377 fn run_secret_demo() -> Result<(), String> { | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:378 let mut registry = SecretRegistry::new(); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:379 let openrouter = SecretHandle("openrouter_api_key".to_string()); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:381 SecretRecord::new( | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:383 SecretClass::LlmApiKey, | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:385 "OpenRouter API key handle; plaintext lives in an external secret backend", | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:387 .with_fingerprint(SecretFingerprint::from_secret( | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:388 "sk-demo-openrouter-secret", | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:392 registry.register(SecretRecord::new( | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:393 SecretHandle("buster_identity_key".to_string()), | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:394 SecretClass::IdentityKey, | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:396 "Buster identity key; not leasable as an ordinary secret", | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:409 println!("registered secret handle: {}", openrouter.0); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:413 println!("plaintext secret exposed: no"); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:423 &SecretHandle("buster_identity_key".to_string()), | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:431 let redactor = SecretRedactor::new(registry.fingerprints(), b"demo-fingerprint-key".to_vec()); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:432 let leaked_line = "adapter log accidentally included sk-demo-openrouter-secret"; | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:442 fn run_secret_store_demo(root: &Path) -> Result<(), String> { | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:444 let store_path = root.join("secrets").join("buster-secrets.json"); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:446 let handle = SecretHandle("openrouter_api_key".to_string()); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:447 let mut store = LocalEncryptedSecretStore::new(&store_path, &master_key); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:448 store.put_secret(handle.clone(), "sk-demo-local-secret".to_string()); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:450 let mut registry = SecretRegistry::new(); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:451 registry.register(SecretRecord::new( | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:453 SecretClass::LlmApiKey, | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:455 "stored in Buster local encrypted secret store", | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:466 let mut broker = SecretBroker::new(registry, store); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:469 .use_secret_once( | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:473 |secret| format!("Bearer len={}", secret.len()), | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:475 .map_err(|error| format!("failed to use secret: {error:?}"))?; | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:478 println!("local secret store: {}", store_path.display()); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:481 raw.contains("sk-demo-local-secret") | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:483 println!("broker received secret shape: {header_shape}"); | mitigation: confirm only secret handles are logged and redaction covers exact values
- [low][network egress review] buster-core/crates/buster_body/src/bin/buster-body.rs:579 "https://evil.example/api", | mitigation: confirm host is allowlisted and request goes through the egress broker
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:693 buster-body secret-demo\n\ | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/bin/buster-body.rs:694 buster-body secret-store-demo --root PROJECT_ROOT\n\ | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/capabilities.rs:12 pub secrets: AccessLevel, | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/cli_tool.rs:15 use crate::secrets::SecretRedactor; | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/cli_tool.rs:27 pub redactor: Option<SecretRedactor>, | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/cli_tool.rs:83 pub fn with_redactor(mut self, redactor: SecretRedactor) -> Self { | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][process or shell boundary review] buster-core/crates/buster_body/src/cli_tool.rs:157 let mut command = Command::new(&self.command.command); | mitigation: confirm arguments are structured and scoped to Buster-owned processes or hosts
- [low][panic surface review] buster-core/crates/buster_body/src/cli_tool.rs:270 let completion = backend.execute(&request).unwrap(); | mitigation: confirm panic cannot be triggered by untrusted input or corrupt state
- [medium][secret handling review] buster-core/crates/buster_body/src/cli_tool.rs:285 let redactor = SecretRedactor::new( | mitigation: confirm only secret handles are logged and redaction covers exact values
- [medium][secret handling review] buster-core/crates/buster_body/src/cli_tool.rs:286 vec![crate::secrets::SecretFingerprint::from_secret( | mitigation: confirm only secret handles are logged and redaction covers exact values
