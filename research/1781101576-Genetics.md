# Secondary Research: Genetics

- task_id: followup-1781020117-697148a74ca6
- question: What small local computation, simulation, or table could Buster run next to test part of this question: What small local computation, simulation, or table could Buster run next to test part of this question: How does the choice of PRS method (e.g. P+T, LDpred2, PRS-CSx, XP-BLUP) affect performance in under-represented populations when the training sample is fixed???
- source_type: llm_secondary_with_fetched_evidence
- needs_verification: true
- authority: hypothesis_or_explanation
- source_count: 9
- source_bundle: ../research/sources/1781101576-Genetics-followup-1781020117-697148a74ca6.json

**Research-chain note: PRS method performance in under-represented populations**

*Note: All claims below are based on secondary model synthesis and general genetics knowledge unless a verifiable source is explicitly cited. Treat as hypotheses requiring validation.*

---

### 1. Follow-up questions
- Can a simple ancestry-aware simulation using 1000 Genomes Project data show that P+T fails to capture cross-ancestry LD patterns compared to PRS-CSx or XP-BLUP?
- How much does tuning the LD reference panel (global vs. ancestry-matched) affect PRS-CSx performance in a simulated under-represented population?
- What is the minimal sample size (in a simulated population) where XP-BLUP’s shared component model starts outperforming P+T?
- Does a simple linear combination of ancestry-specific PRS (using fixed weights) outperform P+T in a cross-ancestry simulation?
- Could a basic simulation show that including a small number of population-specific causal variants dramatically changes method rankings?

### 2. Verification leads (mark uncertain)
1. **Paper:** “Polygenic risk score methods for diverse populations” (2022) by Márquez-Luna et al. [verify via PubMed/Google Scholar]
2. **Dataset:** 1000 Genomes Project Phase 3 (publicly available) – for simulating population structure and LD patterns.
3. **Tool:** PRS-CSx software documentation and example simulations (GitHub repository).
4. **Simulation tool:** `simuPOP` or `SLiM` for generating genotype-phenotype data with controlled ancestry.
5. **Code:** Existing benchmarks in the `PRS-CSx` or `LDpred2` repositories may contain simulation scripts.
6. **Uncertain:** Whether any published simulation uses exactly fixed training sample size across methods – check methods sections of benchmark papers.

### 3. Claim table

| Claim | Source support | Uncertainty | Falsifier |
|-------|---------------|-------------|-----------|
| LD mismatch between training and target populations reduces PRS accuracy more for P+T (which uses sample LD) than for Bayesian methods (which use reference LD panels). | General genetics knowledge; inferred from LD structure discussions in PRS method papers. | High: need direct simulation evidence; unclear magnitude. | Simulation showing P+T outperforms PRS-CSx even with ancestry-matched LD reference panel in under-represented target. |
| XP-BLUP’s shared component can partially capture cross-ancestry genetic effects, improving portability when training sample is diverse but